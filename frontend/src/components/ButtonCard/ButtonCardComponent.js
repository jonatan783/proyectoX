import { Link} from "react-router-dom";

const ButtonsCardComponent = (data) => {
  return (
    <Link to="">
      <button className="button">{data}</button>
    </Link>
  );
};

export default ButtonsCardComponent;

//sin uso, ignorar.