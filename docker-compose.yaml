services:
  #db:
  #  image: postgres:10
  #  environment:
  #    POSTGRES_DB: "grow"
  #    POSTGRES_USER: "sativa"
  #    POSTGRES_PASSWORD: "sativa"
  #  ports:
  #    - "5432"
  central_api:
    build: ./backend
    ports:
      - "3001:3001"
    volumes:
      - "./backend/api:/src"
    env_file:
      - "./backend/api/.env"
    #depends_on:
    #  - db
  client:
    build: ./frontend
    ports:
      - "3000:3000"
    volumes:
      - "./frontend/public:/app/public"
      - "./frontend/src:/app/src"